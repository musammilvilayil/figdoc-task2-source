{
  "name": "figdoc",
  "version": "1.0.0",
  "private": true,
  "description": "Convert Figma design files into structured, editable content documentation.",
  "workspaces": ["client", "server"],
  "scripts": {
    "dev": "concurrently -n api,web -c cyan,magenta \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "start": "npm run start -w server",
    "test": "npm run test -w server",
    "check": "npm run lint -w client && npm run test -w server && npm run build -w client"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "engines": {
    "node": ">=20"
  }
}
